# Sira-Tap
We're building Sirah MapTap, a daily geography-guessing game teaching Islamic sirah history, using the attached spec as the source of truth. Treat its decisions as settled — don't re-propose alternatives already decided there. Flag me directly if something in a request would contradict the spec, rather than silently going along with it.
